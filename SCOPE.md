v1 = Windows desktop app + backend + analysis worker

Must have: audio capture (mic+system), local transcription, session save, upload transcript, queued analysis, report UI

Must NOT have: real-time coaching, multi-coach, teams, payments, mobile, storing raw audio in cloud